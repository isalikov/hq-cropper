import { IClassNames } from '@src/types'
import { getClassName, getRandomString } from '@src/utils'

export const initialClassNames: IClassNames = {
    container: getClassName(),
}

export default initialClassNames
